services:
  trendarr:
    image: ghcr.io/sternpaul/trendarr:latest
    # build: . # Uncomment to build locally from source instead of pulling image
    container_name: trendarr
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/usr/src/app/data
    # Optional: If running on the same Docker network as Radarr & Sonarr, 
    # you can uncomment this to communicate natively without exposing host ports
    # networks:
    #   - arr-network

# networks:
#   arr-network:
#     external: true

